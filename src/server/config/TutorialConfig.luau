-- Owner ID verified in WordSearch/shared/config/GameConfig (Word Hunt).
return {
	OwnerUserId = 1790165114,
	ReplayEveryJoin = false,
	-- Owner only: the tutorial is marked complete on every join.
	SkipTutorial = true,
	ResetProgressEveryJoin = true,
}
